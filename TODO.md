# 東美游泳隊網站 V2 待辦

## 已完成：本機程式與介面

- [x] 先檢查網站、成績匯入程式、目前資料、Firebase 管理功能與 Git 狀態，再決定改動範圍。
- [x] 記錄現況、風險、資料表設計與分階段遷移方式，見 [`docs/TMSC-V2-MIGRATION.md`](docs/TMSC-V2-MIGRATION.md)。
- [x] 建立會員、選手、成績、賽事、分段、文章、分類、標籤、日期、附件與同步紀錄的資料表設計。
- [x] 為會員文章、文章標題、附件資料與私有檔案加入資料庫及檔案存取規則；頁面隱藏只負責呈現，真正權限由資料庫檢查。
- [x] 建立可重複執行的成績匯入；以來源識別碼更新資料，保留原始來源欄位，並按選手和成績的可見設定篩出公開快照。
- [x] 保持原首頁與成績查詢介面；選手、升學及管理新頁統一使用首頁既有海軍藍、水藍、白色與金色，並加入樣式版本參數確保更新可見。
- [x] 建立選手名單與個人資料頁，依實際成績呈現個人最佳、項目最佳、趨勢、年度進步、近期成績與比賽紀錄；來源沒有分段資料就不顯示分段分析。
- [x] 在登入後的隊內功能加入「選手資料」入口；會員和公開訪客可見哪些成績由資料庫權限決定。
- [x] 建立隊內升學閱讀頁、私有圖片與 PDF 閱讀、文章管理、會員邀請／停用，以及管理員同步入口。
- [x] 加入忘記密碼頁，重設信指定返回升學登入頁；忘記登入信箱時提示聯絡教練，避免公開帳號是否存在。
- [x] 為邀請、會員停用與同步派送加入管理員身分檢查；同步工作一次只允許一筆，並設冷卻時間。
- [x] 檢查成績來源使用條款。由於目前條款禁止大量自動擷取，已在本機移除同步排程，並在未獲來源授權時阻止爬蟲執行。
- [x] 用本機模擬服務驗證 9,908 筆資料可以匯入、重複執行不會重複新增，且非公開選手／成績不會留在公開快照。
- [x] 將 Firebase 設定移出受 Git 追蹤的前端程式，改從被忽略的本機 runtime 設定載入；沒有設定時顯示服務未設定狀態。
- [x] 收緊賽事與分段資料的可見權限，必須同時符合成績與選手權限。
- [x] 讓匿名權限政策可安全呼叫角色判斷函式；訪客只能得到「非會員」結果，不會讀到會員資料。
- [x] 在 crawler 本身加入來源授權檢查，避免繞過 GitHub Actions 直接執行未核准擷取。
- [x] 將原 Firebase `honours` 集合中的 7 筆紀錄，以舊文件 ID 作為遷移識別，匯入 Supabase `site_honours`；首頁可讀取 Supabase 公開資料。
- [x] 將首頁原有 4 個招生班別納入 Supabase `recruitment_classes`，並在新版管理台加入最新動態、榮譽與招生資訊管理。
- [x] 檢查舊 Firebase `news` 集合，確認目前沒有動態資料；Supabase 最新動態資料表目前為 0 筆。
- [x] 已盤點兩個 Firebase 專案的服務：`ScoreManager` 沒有 Auth 使用者或 Firestore 資料，Realtime Database 為空；Hosting 尚未初始化，Functions 和 Storage 在目前 Spark 方案未啟用，Analytics 顯示無資料。
- [x] 主 Firebase 專案目前仍有使用跡象：Firestore 儀表板顯示最近 7 天有 7 次讀取；Hosting 未初始化、Storage 與 Functions 未啟用。GitHub Pages 部署新前端並驗證前，不能刪除該專案。
- [ ] [注意：刪除前要先核對帳號歸屬] `dongmei-swim` 仍有 4 個 Firebase Auth 帳號；不可直接刪除，需先由管理員確認哪些人要邀請到 Supabase，再完成登入驗證。
- [x] 確認 Supabase 匿名可讀取公開榮譽與招生資料；匿名讀取升學文章及附件索引沒有取得資料。
- [x] 完成正式網站用的 Supabase Edge Function 代理程式碼；瀏覽器不再需要 API key，僅允許特定來源和必要的 Auth／REST／Storage／管理函式路徑，並封鎖匿名註冊與管理 API 路徑。
- [x] 限定直接使用本機 publishable key 的模式只能在 localhost；加入代理路徑、權限標頭轉送、CORS 與禁止跨站目標的自動測試。
- [x] 管理附件先向 Supabase 取得限時上傳網址，再經受限代理上傳，不在瀏覽器加入 API key。
- [x] 將 Supabase 預設公開端點設為預定的 Edge Function 代理；端點網址不含 API key。
- [x] 已部署 `tmsc-api`；確認本機與正式來源可連線、公開選手資料可讀、未登入文章查詢被拒絕、匿名密碼登入端點可回應、非允許來源遭拒絕，且管理／註冊路徑遭封鎖。
- [ ] [進行中：需管理員登入測試] 部署 `manage-member`、`dispatch-swim-sync`，並以有效管理員、一般會員及停用帳號驗證後台權限、文章與附件管理。
- [x] 通過 JavaScript 語法、現有成績快照、GitHub Actions YAML、差異格式，以及桌機與手機頁面錯誤／橫向溢出檢查。

## 尚待外部設定或核准

- [x] 輪替 Supabase 後端 secret key；帳戶持有人已刪除舊 key，匯入暫存檔與瀏覽器剪貼簿已清除。
- [ ] [暫緩：需來源方核准或可合法使用的替代來源] 取得 Swim Insights 對自動匯入的明確授權，或改用核准的官方資料來源／匯出檔，再啟用同步功能。
- [x] 建立 Supabase 免費方案專案（首爾區域），關閉公開註冊並設定教育頁導回網址。
- [x] 套用資料庫基礎 migration 與後端 `service_role` 權限 migration；15 張資料表已啟用 RLS，教育附件空間維持 private。
- [x] 匯入現有成績快照：9,908 筆成績、354 位選手、197 場賽事；首次匯入新增 9,908 筆、重複 0 筆，影響 354 位選手。
- [x] 從 Supabase 實際資料庫核對筆數一致，匿名請求無法讀取升學文章、附件索引、分類、標籤或日期；education bucket 為 private。
- [x] 已依帳戶持有人指定的信箱寄出首位教練管理員邀請，並在 profiles 設為啟用中的 admin。
- [x] 管理員已完成邀請信確認；資料庫確認 email 已驗證，角色為啟用中的 admin。
- [ ] 用戶在本機預覽以已確認的管理員帳號登入，測試新版管理台新增／編輯公開資訊、文章管理與會員功能；成績同步須等來源授權及 GitHub 派送密鑰設定後再測。
- [ ] [暫緩：需真實資料庫與四種測試身分] 測試資料表與私有檔案權限，確認未登入者拿不到升學文章標題、正文、附件資料或檔案。
- [ ] [暫緩：需先部署代理] 從本機設定切換至正式 Edge Function 代理；service-role、publishable key、GitHub 和資料來源憑證都留在伺服器密鑰管理處。
- [ ] [暫緩：需來源授權、GitHub Actions 密鑰與函式部署] 用管理台完成一次核准來源同步，核對新增、更新、重複、錯誤、影響選手數與最後同步時間。
- [ ] [暫緩：需先完成新版管理台端對端驗證] 將舊 Firebase 後台入口導向新版管理台；目前舊入口保留作回復途徑。
- [ ] [暫緩：需盤點剩餘服務並在刪除當下再次確認] 停用／刪除已完成遷移且確認無其他用途的 Firebase 專案；`dongmei-swim` 是目前網站設定引用的專案。`ScoreManager` 的 Hosting 網址目前顯示 Site Not Found、Firestore 尚未建立、Realtime Database 根節點為空，但仍有註冊的網頁應用程式；Authentication、Storage、Functions 等服務用途尚未核對。
- [x] 已依帳戶持有人指示推送網站與 V2 前端到 GitHub Pages；線上更新完成後仍需由帳戶持有人實際測試會員登入與主要頁面。

## 不可偏離的原則

- 會員文章及附件不放進公開 HTML、JavaScript、JSON 或公開儲存空間。
- 成績分析只根據來源資料；沒有的分段、能力分數或賽事資訊不自行補造。
- 保留現有功能與回復路徑，直到替代流程通過資料核對與權限測試。
- 不增加留言、論壇、聊天室、按讚、商城或聊天機器人等非核心功能。

## 後續可加：Google 表單會員申請

- [ ] 用 Google 表單收集申請者姓名與電子郵件；不收密碼或非必要個資，表單回覆試算表僅限管理者存取。
- [ ] 在 ADMIN 後台加入 CSV 預覽匯入：先檢查欄位、無效信箱與重複申請，讓管理員確認後才寫入 `member_invites` 並寄出 Supabase 邀請。
- [ ] 申請不等於會員：填表者不會自動登入；須收到邀請、驗證信箱並完成密碼設定後，才以一般 `member` 身分啟用。表單不得建立管理員。
- [ ] 支援 Google Forms 回覆匯出 CSV，避免需要 Google API key 或公開回覆試算表；驗證可使用 Google 已驗證信箱或由 Supabase 邀請信完成。
